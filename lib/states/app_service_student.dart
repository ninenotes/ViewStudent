import 'package:flutter/material.dart';

class AppServiceStudent extends StatelessWidget {
  const AppServiceStudent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}