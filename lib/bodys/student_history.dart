import 'package:flutter/material.dart';

class StudenHistory extends StatelessWidget {
  const StudenHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}